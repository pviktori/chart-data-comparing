import { ToListItemsPipe } from './to-list-items.pipe';

describe('ToListItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToListItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
